//! Domain policy for AccountingPost aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use backbone_core::PermitAllPolicy;
use crate::domain::entity::AccountingPost;

/// Domain policy for AccountingPost — permits all operations (no business invariants).
///
/// To add domain rules, replace this alias with a struct implementing
/// `backbone_core::DomainPolicy<AccountingPost>` in the `// <<< CUSTOM` zone.
pub type AccountingPostDomainPolicy = PermitAllPolicy<AccountingPost>;

// <<< CUSTOM
// END CUSTOM
