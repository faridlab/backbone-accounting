//! Repository trait for TaxTagRepairRun aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the TaxTagRepairRun aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::TaxTagRepairRun;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct TaxTagRepairRunPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl TaxTagRepairRunPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct TaxTagRepairRunPaginatedResult {
    pub data: Vec<TaxTagRepairRun>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct TaxTagRepairRunFilter {
    pub lines_examined: Option<i64>,
    pub lines_retagged: Option<i64>,
    pub dry_run: Option<bool>,
    pub actor: Option<Uuid>,
    pub reason: Option<String>,
}

impl TaxTagRepairRunFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.lines_examined.is_some() || self.lines_retagged.is_some() || self.dry_run.is_some() || self.actor.is_some() || self.reason.is_some()
    }
}

/// Repository trait for TaxTagRepairRun aggregate
///
/// This trait defines the persistence contract for TaxTagRepairRun entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait TaxTagRepairRunRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new tax_tag_repair_run entity
    async fn save(&self, entity: &TaxTagRepairRun) -> Result<TaxTagRepairRun>;

    /// Find tax_tag_repair_run by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<TaxTagRepairRun>>;

    /// Find all tax_tag_repair_run entities
    async fn find_all(&self) -> Result<Vec<TaxTagRepairRun>>;

    /// Update tax_tag_repair_run by ID
    async fn update(&self, id: &str, entity: &TaxTagRepairRun) -> Result<Option<TaxTagRepairRun>>;

    /// Delete tax_tag_repair_run by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List tax_tag_repair_run with pagination
    async fn list(&self, params: TaxTagRepairRunPaginationParams) -> Result<TaxTagRepairRunPaginatedResult>;

    /// List tax_tag_repair_run with pagination and filters
    async fn list_with_filters(&self, params: TaxTagRepairRunPaginationParams, filters: TaxTagRepairRunFilter) -> Result<TaxTagRepairRunPaginatedResult>;

    /// Count all tax_tag_repair_run entities
    async fn count(&self) -> Result<u64>;

    /// Count tax_tag_repair_run entities matching filters
    async fn count_with_filters(&self, filters: TaxTagRepairRunFilter) -> Result<u64>;

    /// Check if tax_tag_repair_run exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save tax_tag_repair_run entities
    async fn bulk_save(&self, entities: &[TaxTagRepairRun]) -> Result<Vec<TaxTagRepairRun>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
