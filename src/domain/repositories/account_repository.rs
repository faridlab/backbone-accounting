//! Repository trait for Account aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the Account aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{Account, AccountStatus, AccountSubtype, AccountType, NormalBalance};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct AccountPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl AccountPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct AccountPaginatedResult {
    pub data: Vec<Account>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct AccountFilter {
    pub company_id: Option<Uuid>,
    pub account_number: Option<String>,
    pub account_code: Option<String>,
    pub name: Option<String>,
    pub name_en: Option<String>,
    pub description: Option<String>,
    pub account_type: Option<AccountType>,
    pub account_subtype: Option<AccountSubtype>,
    pub normal_balance: Option<NormalBalance>,
    pub parent_id: Option<Uuid>,
    pub path: Option<String>,
    pub is_header: Option<bool>,
    pub is_detail: Option<bool>,
    pub currency: Option<String>,
    pub bank_name: Option<String>,
    pub bank_account_number: Option<String>,
    pub bank_account_name: Option<String>,
    pub bank_branch: Option<String>,
    pub is_taxable: Option<bool>,
    pub tax_account_id: Option<Uuid>,
    pub is_reconcilable: Option<bool>,
    pub has_budget: Option<bool>,
    pub allow_manual_entry: Option<bool>,
    pub require_cost_center: Option<bool>,
    pub require_project: Option<bool>,
    pub show_in_reports: Option<bool>,
    pub status: Option<AccountStatus>,
    pub is_system: Option<bool>,
    pub notes: Option<String>,
    pub source_id: Option<Uuid>,
    pub is_cloned: Option<bool>,
}

impl AccountFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some() || self.account_number.is_some() || self.account_code.is_some() || self.name.is_some() || self.name_en.is_some() || self.description.is_some() || self.account_type.is_some() || self.account_subtype.is_some() || self.normal_balance.is_some() || self.parent_id.is_some() || self.path.is_some() || self.is_header.is_some() || self.is_detail.is_some() || self.currency.is_some() || self.bank_name.is_some() || self.bank_account_number.is_some() || self.bank_account_name.is_some() || self.bank_branch.is_some() || self.is_taxable.is_some() || self.tax_account_id.is_some() || self.is_reconcilable.is_some() || self.has_budget.is_some() || self.allow_manual_entry.is_some() || self.require_cost_center.is_some() || self.require_project.is_some() || self.show_in_reports.is_some() || self.status.is_some() || self.is_system.is_some() || self.notes.is_some() || self.source_id.is_some() || self.is_cloned.is_some()
    }
}

/// Repository trait for Account aggregate
///
/// This trait defines the persistence contract for Account entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait AccountRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new account entity
    async fn save(&self, entity: &Account) -> Result<Account>;

    /// Find account by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<Account>>;

    /// Find all account entities
    async fn find_all(&self) -> Result<Vec<Account>>;

    /// Update account by ID
    async fn update(&self, id: &str, entity: &Account) -> Result<Option<Account>>;

    /// Delete account by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List account with pagination
    async fn list(&self, params: AccountPaginationParams) -> Result<AccountPaginatedResult>;

    /// List account with pagination and filters
    async fn list_with_filters(&self, params: AccountPaginationParams, filters: AccountFilter) -> Result<AccountPaginatedResult>;

    /// Count all account entities
    async fn count(&self) -> Result<u64>;

    /// Count account entities matching filters
    async fn count_with_filters(&self, filters: AccountFilter) -> Result<u64>;

    /// Check if account exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete account by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted account by ID
    async fn restore(&self, id: &str) -> Result<Option<Account>>;

    /// List soft-deleted account entities
    async fn list_deleted(&self, params: AccountPaginationParams) -> Result<AccountPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save account entities
    async fn bulk_save(&self, entities: &[Account]) -> Result<Vec<Account>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
