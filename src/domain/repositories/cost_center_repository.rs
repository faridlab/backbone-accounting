//! Repository trait for CostCenter aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the CostCenter aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::CostCenter;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct CostCenterPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl CostCenterPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct CostCenterPaginatedResult {
    pub data: Vec<CostCenter>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct CostCenterFilter {
    pub company_id: Option<Uuid>,
    pub code: Option<String>,
    pub name: Option<String>,
    pub name_en: Option<String>,
    pub description: Option<String>,
    pub parent_id: Option<Uuid>,
    pub is_group: Option<bool>,
    pub branch_id: Option<Uuid>,
    pub is_active: Option<bool>,
}

impl CostCenterFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some() || self.code.is_some() || self.name.is_some() || self.name_en.is_some() || self.description.is_some() || self.parent_id.is_some() || self.is_group.is_some() || self.branch_id.is_some() || self.is_active.is_some()
    }
}

/// Repository trait for CostCenter aggregate
///
/// This trait defines the persistence contract for CostCenter entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait CostCenterRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new cost_center entity
    async fn save(&self, entity: &CostCenter) -> Result<CostCenter>;

    /// Find cost_center by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<CostCenter>>;

    /// Find all cost_center entities
    async fn find_all(&self) -> Result<Vec<CostCenter>>;

    /// Update cost_center by ID
    async fn update(&self, id: &str, entity: &CostCenter) -> Result<Option<CostCenter>>;

    /// Delete cost_center by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List cost_center with pagination
    async fn list(&self, params: CostCenterPaginationParams) -> Result<CostCenterPaginatedResult>;

    /// List cost_center with pagination and filters
    async fn list_with_filters(&self, params: CostCenterPaginationParams, filters: CostCenterFilter) -> Result<CostCenterPaginatedResult>;

    /// Count all cost_center entities
    async fn count(&self) -> Result<u64>;

    /// Count cost_center entities matching filters
    async fn count_with_filters(&self, filters: CostCenterFilter) -> Result<u64>;

    /// Check if cost_center exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete cost_center by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted cost_center by ID
    async fn restore(&self, id: &str) -> Result<Option<CostCenter>>;

    /// List soft-deleted cost_center entities
    async fn list_deleted(&self, params: CostCenterPaginationParams) -> Result<CostCenterPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save cost_center entities
    async fn bulk_save(&self, entities: &[CostCenter]) -> Result<Vec<CostCenter>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
