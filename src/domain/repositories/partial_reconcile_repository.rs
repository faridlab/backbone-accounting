//! Repository trait for PartialReconcile aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the PartialReconcile aggregate.
//! Implementation is in the infrastructure layer.

use anyhow::Result;
use async_trait::async_trait;
use uuid::Uuid;

use crate::domain::entity::{PartialReconcile, ReconcileOrigin};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PartialReconcilePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl PartialReconcilePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct PartialReconcilePaginatedResult {
    pub data: Vec<PartialReconcile>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PartialReconcileFilter {
    pub company_id: Option<Uuid>,
    pub debit_move_id: Option<Uuid>,
    pub credit_move_id: Option<Uuid>,
    pub full_reconcile_id: Option<Uuid>,
    pub exchange_move_id: Option<Uuid>,
    pub currency: Option<String>,
    pub origin: Option<ReconcileOrigin>,
    pub source_type: Option<String>,
    pub source_id: Option<Uuid>,
}

impl PartialReconcileFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some()
            || self.debit_move_id.is_some()
            || self.credit_move_id.is_some()
            || self.full_reconcile_id.is_some()
            || self.exchange_move_id.is_some()
            || self.currency.is_some()
            || self.origin.is_some()
            || self.source_type.is_some()
            || self.source_id.is_some()
    }
}

/// Repository trait for PartialReconcile aggregate
///
/// This trait defines the persistence contract for PartialReconcile entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PartialReconcileRepository: Send + Sync {
    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new partial_reconcile entity
    async fn save(&self, entity: &PartialReconcile) -> Result<PartialReconcile>;

    /// Find partial_reconcile by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<PartialReconcile>>;

    /// Find all partial_reconcile entities
    async fn find_all(&self) -> Result<Vec<PartialReconcile>>;

    /// Update partial_reconcile by ID
    async fn update(&self, id: &str, entity: &PartialReconcile)
        -> Result<Option<PartialReconcile>>;

    /// Delete partial_reconcile by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List partial_reconcile with pagination
    async fn list(
        &self,
        params: PartialReconcilePaginationParams,
    ) -> Result<PartialReconcilePaginatedResult>;

    /// List partial_reconcile with pagination and filters
    async fn list_with_filters(
        &self,
        params: PartialReconcilePaginationParams,
        filters: PartialReconcileFilter,
    ) -> Result<PartialReconcilePaginatedResult>;

    /// Count all partial_reconcile entities
    async fn count(&self) -> Result<u64>;

    /// Count partial_reconcile entities matching filters
    async fn count_with_filters(&self, filters: PartialReconcileFilter) -> Result<u64>;

    /// Check if partial_reconcile exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save partial_reconcile entities
    async fn bulk_save(&self, entities: &[PartialReconcile]) -> Result<Vec<PartialReconcile>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
