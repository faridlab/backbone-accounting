//! Repository trait for FiscalPeriod aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the FiscalPeriod aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{FiscalPeriod, PeriodStatus, PeriodType};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct FiscalPeriodPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl FiscalPeriodPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct FiscalPeriodPaginatedResult {
    pub data: Vec<FiscalPeriod>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct FiscalPeriodFilter {
    pub company_id: Option<Uuid>,
    pub period_code: Option<String>,
    pub name: Option<String>,
    pub period_type: Option<PeriodType>,
    pub parent_id: Option<Uuid>,
    pub status: Option<PeriodStatus>,
    pub is_current: Option<bool>,
    pub opening_balance_set: Option<bool>,
    pub opening_balance_by: Option<Uuid>,
    pub closing_started_by: Option<Uuid>,
    pub closed_by: Option<Uuid>,
    pub locked_by: Option<Uuid>,
    pub lock_reason: Option<String>,
    pub allow_adjustments: Option<bool>,
    pub balance_sheet_generated: Option<bool>,
    pub income_statement_generated: Option<bool>,
    pub notes: Option<String>,
}

impl FiscalPeriodFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some() || self.period_code.is_some() || self.name.is_some() || self.period_type.is_some() || self.parent_id.is_some() || self.status.is_some() || self.is_current.is_some() || self.opening_balance_set.is_some() || self.opening_balance_by.is_some() || self.closing_started_by.is_some() || self.closed_by.is_some() || self.locked_by.is_some() || self.lock_reason.is_some() || self.allow_adjustments.is_some() || self.balance_sheet_generated.is_some() || self.income_statement_generated.is_some() || self.notes.is_some()
    }
}

/// Repository trait for FiscalPeriod aggregate
///
/// This trait defines the persistence contract for FiscalPeriod entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait FiscalPeriodRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new fiscal_period entity
    async fn save(&self, entity: &FiscalPeriod) -> Result<FiscalPeriod>;

    /// Find fiscal_period by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<FiscalPeriod>>;

    /// Find all fiscal_period entities
    async fn find_all(&self) -> Result<Vec<FiscalPeriod>>;

    /// Update fiscal_period by ID
    async fn update(&self, id: &str, entity: &FiscalPeriod) -> Result<Option<FiscalPeriod>>;

    /// Delete fiscal_period by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List fiscal_period with pagination
    async fn list(&self, params: FiscalPeriodPaginationParams) -> Result<FiscalPeriodPaginatedResult>;

    /// List fiscal_period with pagination and filters
    async fn list_with_filters(&self, params: FiscalPeriodPaginationParams, filters: FiscalPeriodFilter) -> Result<FiscalPeriodPaginatedResult>;

    /// Count all fiscal_period entities
    async fn count(&self) -> Result<u64>;

    /// Count fiscal_period entities matching filters
    async fn count_with_filters(&self, filters: FiscalPeriodFilter) -> Result<u64>;

    /// Check if fiscal_period exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete fiscal_period by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted fiscal_period by ID
    async fn restore(&self, id: &str) -> Result<Option<FiscalPeriod>>;

    /// List soft-deleted fiscal_period entities
    async fn list_deleted(&self, params: FiscalPeriodPaginationParams) -> Result<FiscalPeriodPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save fiscal_period entities
    async fn bulk_save(&self, entities: &[FiscalPeriod]) -> Result<Vec<FiscalPeriod>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
