//! Repository trait for AccountingPost aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the AccountingPost aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{AccountingPost, PostingSourceType, PostingStatus, PostingType};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct AccountingPostPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl AccountingPostPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct AccountingPostPaginatedResult {
    pub data: Vec<AccountingPost>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct AccountingPostFilter {
    pub company_id: Option<Uuid>,
    pub branch_id: Option<Uuid>,
    pub source_type: Option<PostingSourceType>,
    pub source_id: Option<Uuid>,
    pub source_reference: Option<String>,
    pub journal_id: Option<Uuid>,
    pub posting_type: Option<PostingType>,
    pub posting_status: Option<PostingStatus>,
    pub currency: Option<String>,
    pub error_code: Option<String>,
    pub error_message: Option<String>,
    pub reverses_post_id: Option<Uuid>,
    pub reversed_by_post_id: Option<Uuid>,
    pub posted_by: Option<Uuid>,
    pub notes: Option<String>,
}

impl AccountingPostFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some() || self.branch_id.is_some() || self.source_type.is_some() || self.source_id.is_some() || self.source_reference.is_some() || self.journal_id.is_some() || self.posting_type.is_some() || self.posting_status.is_some() || self.currency.is_some() || self.error_code.is_some() || self.error_message.is_some() || self.reverses_post_id.is_some() || self.reversed_by_post_id.is_some() || self.posted_by.is_some() || self.notes.is_some()
    }
}

/// Repository trait for AccountingPost aggregate
///
/// This trait defines the persistence contract for AccountingPost entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait AccountingPostRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new accounting_post entity
    async fn save(&self, entity: &AccountingPost) -> Result<AccountingPost>;

    /// Find accounting_post by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<AccountingPost>>;

    /// Find all accounting_post entities
    async fn find_all(&self) -> Result<Vec<AccountingPost>>;

    /// Update accounting_post by ID
    async fn update(&self, id: &str, entity: &AccountingPost) -> Result<Option<AccountingPost>>;

    /// Delete accounting_post by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List accounting_post with pagination
    async fn list(&self, params: AccountingPostPaginationParams) -> Result<AccountingPostPaginatedResult>;

    /// List accounting_post with pagination and filters
    async fn list_with_filters(&self, params: AccountingPostPaginationParams, filters: AccountingPostFilter) -> Result<AccountingPostPaginatedResult>;

    /// Count all accounting_post entities
    async fn count(&self) -> Result<u64>;

    /// Count accounting_post entities matching filters
    async fn count_with_filters(&self, filters: AccountingPostFilter) -> Result<u64>;

    /// Check if accounting_post exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete accounting_post by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted accounting_post by ID
    async fn restore(&self, id: &str) -> Result<Option<AccountingPost>>;

    /// List soft-deleted accounting_post entities
    async fn list_deleted(&self, params: AccountingPostPaginationParams) -> Result<AccountingPostPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save accounting_post entities
    async fn bulk_save(&self, entities: &[AccountingPost]) -> Result<Vec<AccountingPost>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
