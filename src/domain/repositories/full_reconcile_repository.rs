//! Repository trait for FullReconcile aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the FullReconcile aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::FullReconcile;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct FullReconcilePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl FullReconcilePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct FullReconcilePaginatedResult {
    pub data: Vec<FullReconcile>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct FullReconcileFilter {
    pub company_id: Option<Uuid>,
}

impl FullReconcileFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some()
    }
}

/// Repository trait for FullReconcile aggregate
///
/// This trait defines the persistence contract for FullReconcile entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait FullReconcileRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new full_reconcile entity
    async fn save(&self, entity: &FullReconcile) -> Result<FullReconcile>;

    /// Find full_reconcile by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<FullReconcile>>;

    /// Find all full_reconcile entities
    async fn find_all(&self) -> Result<Vec<FullReconcile>>;

    /// Update full_reconcile by ID
    async fn update(&self, id: &str, entity: &FullReconcile) -> Result<Option<FullReconcile>>;

    /// Delete full_reconcile by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List full_reconcile with pagination
    async fn list(&self, params: FullReconcilePaginationParams) -> Result<FullReconcilePaginatedResult>;

    /// List full_reconcile with pagination and filters
    async fn list_with_filters(&self, params: FullReconcilePaginationParams, filters: FullReconcileFilter) -> Result<FullReconcilePaginatedResult>;

    /// Count all full_reconcile entities
    async fn count(&self) -> Result<u64>;

    /// Count full_reconcile entities matching filters
    async fn count_with_filters(&self, filters: FullReconcileFilter) -> Result<u64>;

    /// Check if full_reconcile exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save full_reconcile entities
    async fn bulk_save(&self, entities: &[FullReconcile]) -> Result<Vec<FullReconcile>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
