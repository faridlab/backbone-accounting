//! Domain events for AccountingPost entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::AccountingPost;

/// Domain events for AccountingPost — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<AccountingPost>)` variant.
pub type AccountingPostEvent = CrudEvent<AccountingPost>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for AccountingPost events.
pub type AccountingPostEventPublisher = Arc<dyn CrudEventPublisher<AccountingPost> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopAccountingPostEventPublisher = NoOpCrudEventPublisher<AccountingPost>;
