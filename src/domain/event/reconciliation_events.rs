//! Domain events for Reconciliation entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Reconciliation;

/// Domain events for Reconciliation — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Reconciliation>)` variant.
pub type ReconciliationEvent = CrudEvent<Reconciliation>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Reconciliation events.
pub type ReconciliationEventPublisher = Arc<dyn CrudEventPublisher<Reconciliation> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopReconciliationEventPublisher = NoOpCrudEventPublisher<Reconciliation>;
