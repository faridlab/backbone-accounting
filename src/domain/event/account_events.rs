//! Domain events for Account entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Account;

/// Domain events for Account — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Account>)` variant.
pub type AccountEvent = CrudEvent<Account>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Account events.
pub type AccountEventPublisher = Arc<dyn CrudEventPublisher<Account> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopAccountEventPublisher = NoOpCrudEventPublisher<Account>;
