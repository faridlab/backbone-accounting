//! Domain events for FinancialStatement entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::FinancialStatement;

/// Domain events for FinancialStatement — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<FinancialStatement>)` variant.
pub type FinancialStatementEvent = CrudEvent<FinancialStatement>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for FinancialStatement events.
pub type FinancialStatementEventPublisher = Arc<dyn CrudEventPublisher<FinancialStatement> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopFinancialStatementEventPublisher = NoOpCrudEventPublisher<FinancialStatement>;
