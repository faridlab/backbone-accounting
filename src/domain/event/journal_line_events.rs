//! Domain events for JournalLine entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::JournalLine;

/// Domain events for JournalLine — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<JournalLine>)` variant.
pub type JournalLineEvent = CrudEvent<JournalLine>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for JournalLine events.
pub type JournalLineEventPublisher = Arc<dyn CrudEventPublisher<JournalLine> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopJournalLineEventPublisher = NoOpCrudEventPublisher<JournalLine>;
