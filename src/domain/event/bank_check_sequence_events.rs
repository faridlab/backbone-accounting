//! Domain events for BankCheckSequence entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::BankCheckSequence;

/// Domain events for BankCheckSequence — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<BankCheckSequence>)` variant.
pub type BankCheckSequenceEvent = CrudEvent<BankCheckSequence>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for BankCheckSequence events.
pub type BankCheckSequenceEventPublisher = Arc<dyn CrudEventPublisher<BankCheckSequence> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopBankCheckSequenceEventPublisher = NoOpCrudEventPublisher<BankCheckSequence>;
