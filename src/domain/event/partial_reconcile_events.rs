//! Domain events for PartialReconcile entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use crate::domain::entity::PartialReconcile;
use backbone_messaging::CrudEvent;

/// Domain events for PartialReconcile — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<PartialReconcile>)` variant.
pub type PartialReconcileEvent = CrudEvent<PartialReconcile>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for PartialReconcile events.
pub type PartialReconcileEventPublisher =
    Arc<dyn CrudEventPublisher<PartialReconcile> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopPartialReconcileEventPublisher = NoOpCrudEventPublisher<PartialReconcile>;
