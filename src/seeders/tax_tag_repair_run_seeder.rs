//! TaxTagRepairRun seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial TaxTagRepairRun data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// TAXTAGREPAIRRUN SEEDER
// ============================================================================

/// Seeder for tax_tag_repair_run
pub struct SeedTaxTagRepairRunSeeder;

impl SeedTaxTagRepairRunSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedTaxTagRepairRunSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedTaxTagRepairRunSeeder {
    fn name(&self) -> &'static str {
        "SeedTaxTagRepairRunSeeder"
    }

    fn order(&self) -> i32 {
        5
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if accounting.tax_tag_repair_runs table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM accounting.tax_tag_repair_runs")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/tax_tag_repair_run_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM accounting.tax_tag_repair_runs")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
