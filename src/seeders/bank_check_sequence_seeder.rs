//! BankCheckSequence seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial BankCheckSequence data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// BANKCHECKSEQUENCE SEEDER
// ============================================================================

/// Seeder for bank_check_sequence
pub struct SeedBankCheckSequenceSeeder;

impl SeedBankCheckSequenceSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedBankCheckSequenceSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedBankCheckSequenceSeeder {
    fn name(&self) -> &'static str {
        "SeedBankCheckSequenceSeeder"
    }

    fn order(&self) -> i32 {
        3
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if accounting.bank_check_sequences table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM accounting.bank_check_sequences")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/bank_check_sequence_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM accounting.bank_check_sequences")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
