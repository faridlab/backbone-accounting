//! AccountingPost seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial AccountingPost data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// ACCOUNTINGPOST SEEDER
// ============================================================================

/// Seeder for accounting_post
pub struct SeedAccountingPostSeeder;

impl SeedAccountingPostSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedAccountingPostSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedAccountingPostSeeder {
    fn name(&self) -> &'static str {
        "SeedAccountingPostSeeder"
    }

    fn order(&self) -> i32 {
        1
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if accounting_posts table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM accounting_posts")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/accounting_post_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM accounting_posts")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

