//! EmvQrConfig seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial EmvQrConfig data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// EMVQRCONFIG SEEDER
// ============================================================================

/// Seeder for emv_qr_config
pub struct SeedEmvQrConfigSeeder;

impl SeedEmvQrConfigSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedEmvQrConfigSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedEmvQrConfigSeeder {
    fn name(&self) -> &'static str {
        "SeedEmvQrConfigSeeder"
    }

    fn order(&self) -> i32 {
        2
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if accounting.emv_qr_configs table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM accounting.emv_qr_configs")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/emv_qr_config_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM accounting.emv_qr_configs")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
