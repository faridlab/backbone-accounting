//! Repository for FiscalPeriod entities
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<FiscalPeriod, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::FiscalPeriod;

/// Table name for FiscalPeriod entities
pub const TABLE_NAME: &str = "fiscal_periods";

/// Repository for FiscalPeriod entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct FiscalPeriodRepository(
    backbone_orm::GenericCrudRepository<FiscalPeriod, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for FiscalPeriodRepository {
    type Target = backbone_orm::GenericCrudRepository<FiscalPeriod, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl FiscalPeriodRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

backbone_core::impl_crud_repository!(FiscalPeriodRepository, FiscalPeriod, soft_delete);
