//! Repository for ReconciliationItem entities
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<ReconciliationItem, backbone_orm::HardDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::ReconciliationItem;

/// Table name for ReconciliationItem entities
pub const TABLE_NAME: &str = "accounting.reconciliation_items";

/// Repository for ReconciliationItem entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct ReconciliationItemRepository(
    backbone_orm::GenericCrudRepository<ReconciliationItem, backbone_orm::HardDelete>,
);

impl std::ops::Deref for ReconciliationItemRepository {
    type Target = backbone_orm::GenericCrudRepository<ReconciliationItem, backbone_orm::HardDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl ReconciliationItemRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

backbone_core::impl_crud_repository!(ReconciliationItemRepository, ReconciliationItem, no_soft_delete);
