//! Repository for Reconciliation entities
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<Reconciliation, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::Reconciliation;

/// Table name for Reconciliation entities
pub const TABLE_NAME: &str = "reconciliations";

/// Repository for Reconciliation entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct ReconciliationRepository(
    backbone_orm::GenericCrudRepository<Reconciliation, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for ReconciliationRepository {
    type Target = backbone_orm::GenericCrudRepository<Reconciliation, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl ReconciliationRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

backbone_core::impl_crud_repository!(ReconciliationRepository, Reconciliation, soft_delete);
