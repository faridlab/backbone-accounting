//! Auth configuration for AccountingPost handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<AccountingPost>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::AccountingPostService;
use crate::domain::entity::AccountingPost;

/// Permission string constants for AccountingPost operations.
pub mod permissions {
    pub const LIST:        &str = "accounting_post:list";
    pub const READ:        &str = "accounting_post:read";
    pub const CREATE:      &str = "accounting_post:create";
    pub const UPDATE:      &str = "accounting_post:update";
    pub const DELETE:      &str = "accounting_post:delete";
    pub const RESTORE:     &str = "accounting_post:restore";
    pub const TRASH:       &str = "accounting_post:trash";
    pub const EMPTY_TRASH: &str = "accounting_post:empty_trash";
    pub const BULK_CREATE: &str = "accounting_post:bulk_create";
    pub const UPSERT:      &str = "accounting_post:upsert";
}

/// Resource policy for AccountingPost — maps CRUD actions to permission strings.
pub struct AccountingPostPolicy;

#[async_trait]
impl ResourcePolicy<AccountingPost> for AccountingPostPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &AccountingPost,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for AccountingPost handlers.
///
/// Wraps `AccountingPostService` and checks `AccountingPostPolicy` before delegating.
pub type AccountingPostGuard = PermissionGuard<AccountingPost, AccountingPostService, AccountingPostPolicy>;

/// Service-integrated permission guard for AccountingPost (for use in generated handlers).
pub type AccountingPostServiceGuard = ServicePermissionGuard<AccountingPost, AccountingPostService, AccountingPostPolicy>;

// <<< CUSTOM
// END CUSTOM
