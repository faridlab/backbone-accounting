//! Auth configuration for Account handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<Account>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::AccountService;
use crate::domain::entity::Account;

/// Permission string constants for Account operations.
pub mod permissions {
    pub const LIST:        &str = "account:list";
    pub const READ:        &str = "account:read";
    pub const CREATE:      &str = "account:create";
    pub const UPDATE:      &str = "account:update";
    pub const DELETE:      &str = "account:delete";
    pub const RESTORE:     &str = "account:restore";
    pub const TRASH:       &str = "account:trash";
    pub const EMPTY_TRASH: &str = "account:empty_trash";
    pub const BULK_CREATE: &str = "account:bulk_create";
    pub const UPSERT:      &str = "account:upsert";
}

/// Resource policy for Account — maps CRUD actions to permission strings.
pub struct AccountPolicy;

#[async_trait]
impl ResourcePolicy<Account> for AccountPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &Account,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for Account handlers.
///
/// Wraps `AccountService` and checks `AccountPolicy` before delegating.
pub type AccountGuard = PermissionGuard<Account, AccountService, AccountPolicy>;

/// Service-integrated permission guard for Account (for use in generated handlers).
pub type AccountServiceGuard = ServicePermissionGuard<Account, AccountService, AccountPolicy>;

// <<< CUSTOM
// END CUSTOM
