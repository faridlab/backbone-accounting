//! Auth configuration for ReconciliationItem handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<ReconciliationItem>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::ReconciliationItemService;
use crate::domain::entity::ReconciliationItem;

/// Permission string constants for ReconciliationItem operations.
pub mod permissions {
    pub const LIST:        &str = "reconciliation_item:list";
    pub const READ:        &str = "reconciliation_item:read";
    pub const CREATE:      &str = "reconciliation_item:create";
    pub const UPDATE:      &str = "reconciliation_item:update";
    pub const DELETE:      &str = "reconciliation_item:delete";
    pub const RESTORE:     &str = "reconciliation_item:restore";
    pub const TRASH:       &str = "reconciliation_item:trash";
    pub const EMPTY_TRASH: &str = "reconciliation_item:empty_trash";
    pub const BULK_CREATE: &str = "reconciliation_item:bulk_create";
    pub const UPSERT:      &str = "reconciliation_item:upsert";
}

/// Resource policy for ReconciliationItem — maps CRUD actions to permission strings.
pub struct ReconciliationItemPolicy;

#[async_trait]
impl ResourcePolicy<ReconciliationItem> for ReconciliationItemPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &ReconciliationItem,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for ReconciliationItem handlers.
///
/// Wraps `ReconciliationItemService` and checks `ReconciliationItemPolicy` before delegating.
pub type ReconciliationItemGuard = PermissionGuard<ReconciliationItem, ReconciliationItemService, ReconciliationItemPolicy>;

/// Service-integrated permission guard for ReconciliationItem (for use in generated handlers).
pub type ReconciliationItemServiceGuard = ServicePermissionGuard<ReconciliationItem, ReconciliationItemService, ReconciliationItemPolicy>;

// <<< CUSTOM
// END CUSTOM
