//! Event subscriber type aliases for Accounting module
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::GenericEventSubscriber;

use crate::domain::event::AccountEvent;
use crate::domain::event::AccountingPostEvent;
use crate::domain::event::FinancialStatementEvent;
use crate::domain::event::FiscalPeriodEvent;
use crate::domain::event::JournalEvent;
use crate::domain::event::JournalLineEvent;
use crate::domain::event::LedgerEvent;
use crate::domain::event::ReconciliationEvent;
use crate::domain::event::ReconciliationItemEvent;

/// Subscriber for Account events.
///
/// Register handlers via `GenericEventSubscriber::new(topics, handlers)`.
pub type AccountEventSubscriber = GenericEventSubscriber<AccountEvent>;

/// Subscriber for AccountingPost events.
///
/// Register handlers via `GenericEventSubscriber::new(topics, handlers)`.
pub type AccountingPostEventSubscriber = GenericEventSubscriber<AccountingPostEvent>;

/// Subscriber for FinancialStatement events.
///
/// Register handlers via `GenericEventSubscriber::new(topics, handlers)`.
pub type FinancialStatementEventSubscriber = GenericEventSubscriber<FinancialStatementEvent>;

/// Subscriber for FiscalPeriod events.
///
/// Register handlers via `GenericEventSubscriber::new(topics, handlers)`.
pub type FiscalPeriodEventSubscriber = GenericEventSubscriber<FiscalPeriodEvent>;

/// Subscriber for Journal events.
///
/// Register handlers via `GenericEventSubscriber::new(topics, handlers)`.
pub type JournalEventSubscriber = GenericEventSubscriber<JournalEvent>;

/// Subscriber for JournalLine events.
///
/// Register handlers via `GenericEventSubscriber::new(topics, handlers)`.
pub type JournalLineEventSubscriber = GenericEventSubscriber<JournalLineEvent>;

/// Subscriber for Ledger events.
///
/// Register handlers via `GenericEventSubscriber::new(topics, handlers)`.
pub type LedgerEventSubscriber = GenericEventSubscriber<LedgerEvent>;

/// Subscriber for Reconciliation events.
///
/// Register handlers via `GenericEventSubscriber::new(topics, handlers)`.
pub type ReconciliationEventSubscriber = GenericEventSubscriber<ReconciliationEvent>;

/// Subscriber for ReconciliationItem events.
///
/// Register handlers via `GenericEventSubscriber::new(topics, handlers)`.
pub type ReconciliationItemEventSubscriber = GenericEventSubscriber<ReconciliationItemEvent>;

// <<< CUSTOM
// END CUSTOM
