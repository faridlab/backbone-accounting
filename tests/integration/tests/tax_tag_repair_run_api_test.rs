//! TaxTagRepairRun API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the TaxTagRepairRun CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// TAXTAGREPAIRRUN TEST DATA GENERATOR
// ============================================================================

/// Test data generator for TaxTagRepairRun
pub struct TaxTagRepairRunTestData;

impl TestDataGenerator for TaxTagRepairRunTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "date_from": Utc::now().format("%Y-%m-%d").to_string(),
            "date_to": Utc::now().format("%Y-%m-%d").to_string(),
            "rules": json!({}),
            "lines_examined": 1,
            "lines_retagged": 1,
            "dry_run": false,
            "closed_periods_overridden": json!({}),
            "actor": null,
            "reason": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "ran_at": now,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "date_from": Utc::now().format("%Y-%m-%d").to_string(),
            "date_to": Utc::now().format("%Y-%m-%d").to_string(),
            "rules": json!({}),
            "lines_examined": 1,
            "lines_retagged": 1,
            "dry_run": false,
            "closed_periods_overridden": json!({}),
            "actor": null,
            "reason": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "ran_at": now,
            "updated_at": now,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// TAXTAGREPAIRRUN API TEST
// ============================================================================

/// TaxTagRepairRun API test suite
pub struct TaxTagRepairRunApiTest {
    inner: GenericCrudTest<TaxTagRepairRunTestData>,
}

impl TaxTagRepairRunApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/tax_tag_repair_runs", "TaxTagRepairRun");
        config.supports_soft_delete = false;
        Self {
            inner: GenericCrudTest::new(config, TaxTagRepairRunTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for TaxTagRepairRunApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_tax_tag_repair_run_crud() {
        let mut test = TaxTagRepairRunApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add TaxTagRepairRun specific tests here
// <<< CUSTOM TESTS END >>>
