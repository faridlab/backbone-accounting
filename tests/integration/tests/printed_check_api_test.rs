//! PrintedCheck API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the PrintedCheck CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// PRINTEDCHECK TEST DATA GENERATOR
// ============================================================================

/// Test data generator for PrintedCheck
pub struct PrintedCheckTestData;

impl TestDataGenerator for PrintedCheckTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "bank_account_id": Uuid::new_v4().to_string(),
            "payment_id": Uuid::new_v4().to_string(),
            "payment_number": null,
            "check_number": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "amount": 0,
            "payee_name": null,
            "status": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "printed_at": now,
            "printed_by": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "bank_account_id": Uuid::new_v4().to_string(),
            "payment_id": Uuid::new_v4().to_string(),
            "payment_number": null,
            "check_number": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "amount": 0,
            "payee_name": null,
            "status": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "printed_at": now,
            "printed_by": null,
            "updated_at": now,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// PRINTEDCHECK API TEST
// ============================================================================

/// PrintedCheck API test suite
pub struct PrintedCheckApiTest {
    inner: GenericCrudTest<PrintedCheckTestData>,
}

impl PrintedCheckApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/printed_checks", "PrintedCheck");
        config.supports_soft_delete = false;
        Self {
            inner: GenericCrudTest::new(config, PrintedCheckTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for PrintedCheckApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_printed_check_crud() {
        let mut test = PrintedCheckApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add PrintedCheck specific tests here
// <<< CUSTOM TESTS END >>>
