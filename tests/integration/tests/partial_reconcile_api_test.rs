//! PartialReconcile API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the PartialReconcile CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// PARTIALRECONCILE TEST DATA GENERATOR
// ============================================================================

/// Test data generator for PartialReconcile
pub struct PartialReconcileTestData;

impl TestDataGenerator for PartialReconcileTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "company_id": Uuid::new_v4().to_string(),
            "debit_move_id": Uuid::new_v4().to_string(),
            "credit_move_id": Uuid::new_v4().to_string(),
            "full_reconcile_id": null,
            "exchange_move_id": null,
            "amount": 0,
            "debit_amount_currency": null,
            "credit_amount_currency": null,
            "currency": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "max_date": Utc::now().format("%Y-%m-%d").to_string(),
            "origin": "settlement",
            "source_type": null,
            "source_id": null,
            "metadata": null,
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "company_id": Uuid::new_v4().to_string(),
            "debit_move_id": Uuid::new_v4().to_string(),
            "credit_move_id": Uuid::new_v4().to_string(),
            "full_reconcile_id": null,
            "exchange_move_id": null,
            "amount": 0,
            "debit_amount_currency": null,
            "credit_amount_currency": null,
            "currency": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "max_date": Utc::now().format("%Y-%m-%d").to_string(),
            "origin": "settlement",
            "source_type": null,
            "source_id": null,
            "updated_at": now,
            "metadata": null,
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }

    async fn seed_dependencies(&self, api: &ApiTest) -> Vec<(String, String)> {
        let mut deps: Vec<(String, String)> = Vec::new();
        if let Some(id) = super::crud_test_base::create_and_get_id(
            api,
            "/api/v1/journal_lines",
            &super::journal_line_api_test::JournalLineTestData,
        )
        .await
        {
            deps.push(("debit_move_id".to_string(), id));
        }
        if let Some(id) = super::crud_test_base::create_and_get_id(
            api,
            "/api/v1/journal_lines",
            &super::journal_line_api_test::JournalLineTestData,
        )
        .await
        {
            deps.push(("credit_move_id".to_string(), id));
        }
        deps
    }
}

// ============================================================================
// PARTIALRECONCILE API TEST
// ============================================================================

/// PartialReconcile API test suite
pub struct PartialReconcileApiTest {
    inner: GenericCrudTest<PartialReconcileTestData>,
}

impl PartialReconcileApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/partial_reconciles", "PartialReconcile");
        config.supports_soft_delete = false;
        Self {
            inner: GenericCrudTest::new(config, PartialReconcileTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for PartialReconcileApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_partial_reconcile_crud() {
        let mut test = PartialReconcileApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add PartialReconcile specific tests here
// <<< CUSTOM TESTS END >>>
