//! BankCheckSequence API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the BankCheckSequence CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// BANKCHECKSEQUENCE TEST DATA GENERATOR
// ============================================================================

/// Test data generator for BankCheckSequence
pub struct BankCheckSequenceTestData;

impl TestDataGenerator for BankCheckSequenceTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "bank_account_id": Uuid::new_v4().to_string(),
            "numbering_mode": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "next_number": 1,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "bank_account_id": Uuid::new_v4().to_string(),
            "numbering_mode": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "next_number": 1,
            "updated_at": now,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// BANKCHECKSEQUENCE API TEST
// ============================================================================

/// BankCheckSequence API test suite
pub struct BankCheckSequenceApiTest {
    inner: GenericCrudTest<BankCheckSequenceTestData>,
}

impl BankCheckSequenceApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/bank_check_sequences", "BankCheckSequence");
        config.supports_soft_delete = false;
        Self {
            inner: GenericCrudTest::new(config, BankCheckSequenceTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for BankCheckSequenceApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_bank_check_sequence_crud() {
        let mut test = BankCheckSequenceApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add BankCheckSequence specific tests here
// <<< CUSTOM TESTS END >>>
