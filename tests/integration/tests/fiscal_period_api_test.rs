//! FiscalPeriod API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the FiscalPeriod CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// FISCALPERIOD TEST DATA GENERATOR
// ============================================================================

/// Test data generator for FiscalPeriod
pub struct FiscalPeriodTestData;

impl TestDataGenerator for FiscalPeriodTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "company_id": Uuid::new_v4().to_string(),
            "period_code": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "name": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "period_type": "monthly",
            "start_date": Utc::now().format("%Y-%m-%d").to_string(),
            "end_date": Utc::now().format("%Y-%m-%d").to_string(),
            "fiscal_year": 1,
            "fiscal_quarter": null,
            "fiscal_month": null,
            "parent_id": null,
            "level": 1,
            "status": "open",
            "is_current": false,
            "opening_balance_set": false,
            "opening_balance_date": null,
            "opening_balance_by": null,
            "closing_started_at": now,
            "closing_started_by": null,
            "closed_at": now,
            "closed_by": null,
            "locked_at": now,
            "locked_by": null,
            "lock_reason": null,
            "allow_adjustments": false,
            "adjustment_deadline": null,
            "total_debits": 0,
            "total_credits": 0,
            "journal_count": 1,
            "total_revenue": 0,
            "total_expenses": 0,
            "net_income": 0,
            "total_assets": 0,
            "total_liabilities": 0,
            "total_equity": 0,
            "balance_sheet_generated": false,
            "income_statement_generated": false,
            "statements_generated_at": now,
            "notes": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "company_id": Uuid::new_v4().to_string(),
            "period_code": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "name": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "period_type": "monthly",
            "start_date": Utc::now().format("%Y-%m-%d").to_string(),
            "end_date": Utc::now().format("%Y-%m-%d").to_string(),
            "fiscal_year": 1,
            "fiscal_quarter": null,
            "fiscal_month": null,
            "parent_id": null,
            "level": 1,
            "status": "open",
            "is_current": false,
            "opening_balance_set": false,
            "opening_balance_date": null,
            "opening_balance_by": null,
            "closing_started_at": now,
            "closing_started_by": null,
            "closed_at": now,
            "closed_by": null,
            "locked_at": now,
            "locked_by": null,
            "lock_reason": null,
            "allow_adjustments": false,
            "adjustment_deadline": null,
            "total_debits": 0,
            "total_credits": 0,
            "journal_count": 1,
            "total_revenue": 0,
            "total_expenses": 0,
            "net_income": 0,
            "total_assets": 0,
            "total_liabilities": 0,
            "total_equity": 0,
            "balance_sheet_generated": false,
            "income_statement_generated": false,
            "statements_generated_at": now,
            "notes": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// FISCALPERIOD API TEST
// ============================================================================

/// FiscalPeriod API test suite
pub struct FiscalPeriodApiTest {
    inner: GenericCrudTest<FiscalPeriodTestData>,
}

impl FiscalPeriodApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/fiscal_periods", "FiscalPeriod");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, FiscalPeriodTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for FiscalPeriodApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_fiscal_period_crud() {
        let mut test = FiscalPeriodApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add FiscalPeriod specific tests here
// <<< CUSTOM TESTS END >>>
