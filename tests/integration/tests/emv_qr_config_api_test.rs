//! EmvQrConfig API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the EmvQrConfig CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// EMVQRCONFIG TEST DATA GENERATOR
// ============================================================================

/// Test data generator for EmvQrConfig
pub struct EmvQrConfigTestData;

impl TestDataGenerator for EmvQrConfigTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "merchant_name": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "merchant_city": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "country_code": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "mcc": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "gui": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "merchant_identifier": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "currency": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "initiation_method": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "bank_account_id": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "merchant_name": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "merchant_city": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "country_code": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "mcc": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "gui": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "merchant_identifier": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "currency": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "initiation_method": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "bank_account_id": null,
            "updated_at": now,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// EMVQRCONFIG API TEST
// ============================================================================

/// EmvQrConfig API test suite
pub struct EmvQrConfigApiTest {
    inner: GenericCrudTest<EmvQrConfigTestData>,
}

impl EmvQrConfigApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/emv_qr_configs", "EmvQrConfig");
        config.supports_soft_delete = false;
        Self {
            inner: GenericCrudTest::new(config, EmvQrConfigTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for EmvQrConfigApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_emv_qr_config_crud() {
        let mut test = EmvQrConfigApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add EmvQrConfig specific tests here
// <<< CUSTOM TESTS END >>>
