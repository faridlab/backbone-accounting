//! Ledger API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the Ledger CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// LEDGER TEST DATA GENERATOR
// ============================================================================

/// Test data generator for Ledger
pub struct LedgerTestData;

impl TestDataGenerator for LedgerTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "company_id": Uuid::new_v4().to_string(),
            "account_id": Uuid::new_v4().to_string(),
            "account_number": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "account_name": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "account_type": "asset",
            "normal_balance": "debit",
            "journal_id": Uuid::new_v4().to_string(),
            "journal_number": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "journal_line_id": Uuid::new_v4().to_string(),
            "transaction_date": Utc::now().format("%Y-%m-%d").to_string(),
            "posting_date": Utc::now().format("%Y-%m-%d").to_string(),
            "fiscal_period_id": null,
            "fiscal_year": 1,
            "fiscal_month": 1,
            "description": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "reference": null,
            "currency": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "debit_amount": 0,
            "credit_amount": 0,
            "balance_before": 0,
            "balance_after": 0,
            "balance_change": 0,
            "sequence_number": 1,
            "branch_id": null,
            "party_type": null,
            "party_id": null,
            "cost_center_id": null,
            "project_id": null,
            "department_id": null,
            "dimensions": null,
            "source_type": null,
            "source_id": null,
            "source_reference": null,
            "is_reconciled": false,
            "reconciliation_id": null,
            "reconciled_at": now,
            "is_opening_balance": false,
            "is_closing_entry": false,
            "is_adjustment": false,
            "is_reversed": false,
            "reversed_by_id": null,
            "reverses_id": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "company_id": Uuid::new_v4().to_string(),
            "account_id": Uuid::new_v4().to_string(),
            "account_number": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "account_name": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "account_type": "asset",
            "normal_balance": "debit",
            "journal_id": Uuid::new_v4().to_string(),
            "journal_number": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "journal_line_id": Uuid::new_v4().to_string(),
            "transaction_date": Utc::now().format("%Y-%m-%d").to_string(),
            "posting_date": Utc::now().format("%Y-%m-%d").to_string(),
            "fiscal_period_id": null,
            "fiscal_year": 1,
            "fiscal_month": 1,
            "description": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "reference": null,
            "currency": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "debit_amount": 0,
            "credit_amount": 0,
            "balance_before": 0,
            "balance_after": 0,
            "balance_change": 0,
            "sequence_number": 1,
            "branch_id": null,
            "party_type": null,
            "party_id": null,
            "cost_center_id": null,
            "project_id": null,
            "department_id": null,
            "dimensions": null,
            "source_type": null,
            "source_id": null,
            "source_reference": null,
            "is_reconciled": false,
            "reconciliation_id": null,
            "reconciled_at": now,
            "is_opening_balance": false,
            "is_closing_entry": false,
            "is_adjustment": false,
            "is_reversed": false,
            "reversed_by_id": null,
            "reverses_id": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }

    async fn seed_dependencies(&self, api: &ApiTest) -> Vec<(String, String)> {
        let mut deps: Vec<(String, String)> = Vec::new();
        if let Some(id) = super::crud_test_base::create_and_get_id(api, "/api/v1/accounts", &super::account_api_test::AccountTestData).await {
            deps.push(("account_id".to_string(), id));
        }
        if let Some(id) = super::crud_test_base::create_and_get_id(api, "/api/v1/journals", &super::journal_api_test::JournalTestData).await {
            deps.push(("journal_id".to_string(), id));
        }
        if let Some(id) = super::crud_test_base::create_and_get_id(api, "/api/v1/journal_lines", &super::journal_line_api_test::JournalLineTestData).await {
            deps.push(("journal_line_id".to_string(), id));
        }
        deps
    }
}

// ============================================================================
// LEDGER API TEST
// ============================================================================

/// Ledger API test suite
pub struct LedgerApiTest {
    inner: GenericCrudTest<LedgerTestData>,
}

impl LedgerApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/ledgers", "Ledger");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, LedgerTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for LedgerApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_ledger_crud() {
        let mut test = LedgerApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add Ledger specific tests here
// <<< CUSTOM TESTS END >>>
