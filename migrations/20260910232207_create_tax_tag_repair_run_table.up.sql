-- Migration: Create tax_tag_repair_runs table
-- Generated by metaphor-schema

CREATE SCHEMA IF NOT EXISTS accounting;

CREATE TABLE IF NOT EXISTS accounting.tax_tag_repair_runs (
    id UUID NOT NULL DEFAULT gen_random_uuid(),
    date_from DATE NOT NULL,
    date_to DATE NOT NULL,
    rules JSONB NOT NULL,
    lines_examined BIGINT NOT NULL CHECK (lines_examined >= 0),
    lines_retagged BIGINT NOT NULL CHECK (lines_retagged >= 0),
    dry_run BOOLEAN NOT NULL DEFAULT FALSE,
    closed_periods_overridden JSONB NOT NULL DEFAULT '[]'::jsonb,
    actor UUID,
    reason TEXT NOT NULL,
    ran_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL,
    metadata JSONB NOT NULL DEFAULT '{}'::jsonb,
    PRIMARY KEY (id)
);
