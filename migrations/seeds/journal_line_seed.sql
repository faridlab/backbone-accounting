-- JournalLine Seed Data
-- Generated by metaphor-schema
--
-- This file contains initial seed data for the accounting.journal_lines table.
-- Customize this file to add your initial data.

-- INSERT INTO accounting.journal_lines (
--     id,
--     journal_id,
--     company_id,
--     branch_id,
--     party_type,
--     party_id,
--     line_number,
--     account_id,
--     account_number,
--     account_name,
--     debit_amount,
--     credit_amount,
--     currency,
--     exchange_rate,
--     base_debit_amount,
--     base_credit_amount,
--     description,
--     cost_center_id,
--     project_id,
--     department_id,
--     dimensions,
--     source_type,
--     source_id,
--     source_reference,
--     is_tax_line,
--     tax_rate,
--     tax_base_amount,
--     related_line_id,
--     has_quantity,
--     quantity,
--     unit,
--     unit_price,
--     is_reconciled,
--     reconciliation_id,
--     reconciled_at,
--     is_posted,
--     ledger_id,
--     posted_at,
--     tags,
--     data
-- ) VALUES
-- (
--     -- Add values here
-- );

-- Example: Insert test data
-- Uncomment and modify the following for development/testing:

-- INSERT INTO accounting.journal_lines (id, created_at, updated_at) VALUES
--     (gen_random_uuid(), NOW(), NOW());

-- <<< CUSTOM SEED DATA >>>
-- Add your custom seed data below

