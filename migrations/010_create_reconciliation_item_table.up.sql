-- Migration: Create reconciliation_items table
-- Generated by backbone-schema

CREATE TABLE IF NOT EXISTS reconciliation_items (
    id UUID NOT NULL DEFAULT gen_random_uuid(),
    reconciliation_id UUID NOT NULL,
    provider_id UUID,
    item_number INTEGER NOT NULL,
    source TEXT NOT NULL,
    ledger_id UUID,
    journal_id UUID,
    book_date DATE,
    book_reference TEXT,
    book_description TEXT,
    book_debit NUMERIC NOT NULL DEFAULT 0,
    book_credit NUMERIC NOT NULL DEFAULT 0,
    statement_date DATE,
    statement_reference TEXT,
    statement_description TEXT,
    statement_debit NUMERIC NOT NULL DEFAULT 0,
    statement_credit NUMERIC NOT NULL DEFAULT 0,
    status reconciliation_item_status NOT NULL DEFAULT 'unmatched',
    matched_with_id UUID,
    match_date TIMESTAMPTZ,
    match_method TEXT,
    match_confidence NUMERIC,
    has_difference BOOLEAN NOT NULL DEFAULT FALSE,
    difference_amount NUMERIC NOT NULL DEFAULT 0,
    difference_reason TEXT,
    requires_adjustment BOOLEAN NOT NULL DEFAULT FALSE,
    adjustment_type TEXT,
    adjustment_journal_id UUID,
    adjusted_at TIMESTAMPTZ,
    is_written_off BOOLEAN NOT NULL DEFAULT FALSE,
    write_off_reason TEXT,
    write_off_approved_by UUID,
    is_outstanding BOOLEAN NOT NULL DEFAULT FALSE,
    outstanding_type TEXT,
    expected_clear_date DATE,
    notes TEXT,
    data JSONB NOT NULL DEFAULT '{}'::jsonb,
    PRIMARY KEY (id)
);

CREATE UNIQUE INDEX IF NOT EXISTS idx_reconciliation_items_reconciliation_id_item_number ON reconciliation_items (reconciliation_id, item_number) WHERE deleted_at IS NULL;

CREATE INDEX IF NOT EXISTS idx_reconciliation_items_reconciliation_id_status ON reconciliation_items (reconciliation_id, status);

CREATE INDEX IF NOT EXISTS idx_reconciliation_items_ledger_id ON reconciliation_items (ledger_id) WHERE ledger_id IS NOT NULL;

CREATE INDEX IF NOT EXISTS idx_reconciliation_items_provider_id_is_outstanding ON reconciliation_items (provider_id, is_outstanding);

CREATE INDEX IF NOT EXISTS idx_reconciliation_items_reconciliation_id_source ON reconciliation_items (reconciliation_id, source);

CREATE INDEX IF NOT EXISTS idx_reconciliation_items_matched_with_id ON reconciliation_items (matched_with_id) WHERE matched_with_id IS NOT NULL;

